# Environment Configuration Template
# .env files are generated during deployment from service.yml + GitLab CI/CD Variables
# This file is for reference only

SERVICE_PORT=8080
TZ=America/New_York

# Service-specific variables defined in service.yml secrets section
# are automatically injected from GitLab CI/CD Variables
